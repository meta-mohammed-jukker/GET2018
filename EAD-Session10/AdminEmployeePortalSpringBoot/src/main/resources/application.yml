server:
  servlet:
    context-path: /AdminEmployeePortalSpringBoot
spring:
  datasource: 
    url: jdbc:mysql://localhost/employeePortalDatabase
    username: root
    password: mohammed
    driver-class-name : com.mysql.jdbc.Driver
    type: org.apache.tomcat.jdbc.pool.DataSource
  mail:
    host: smtp.gmail.com
    port: 587
    username: employee.portal.training@gmail.com
    password: jdhuceocjruudbxq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true